# ============================================================
# deslop 白名单（方向 G · 词表豁免层）
# ============================================================
# 一行一词；以 # 开头的整行视为注释；空行忽略。
#
# 用途：命中 config/banned.json 硬禁用项（标点 / 正则 / 模板）但属
#       「世界观术语 / 角色绰号 / 口头禅」等合法用法的，在此列出可豁免。
#       与「」引号语境豁免（已实现）正交：引号＝语境豁免，白名单＝词表豁免。
#
# 机制：仅作用于 banned.json 硬禁用项，不豁免软 AIISH 阈值信号；
#       默认空表 = 零行为变更（不会误杀任何现有检测）。
# 匹配规则：白名单词 wl 出现在被命中文本的任意位置即豁免（wl in matched）。
#
# 示例（按需取消注释，或自行增删）：
# 眼中闪过
# 瞳孔收缩
# 命运齿轮
# 心中一凛
